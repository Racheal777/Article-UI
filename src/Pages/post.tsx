import React, { useState, useEffect } from "react";
import "../App.css";

function Todo() {
  const [ check, setCheck ] = useState(false);

  // console.log(setCheck)
  // useEffect(() => {
  //   console.log(check);
  // }, [check]);
  return (
    <div>
      
    </div>
  );
}

export default Todo;
